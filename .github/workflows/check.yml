# Verifies the exact published head, as DEVELOPMENT.md requires:
#   "CI verifies the exact published head."
#   "Merge always requires an explicit decision from a current maintainer."
# This workflow reports status. It merges nothing.
name: check

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Minste rettighet som trengs for å lese koden og rapportere status.
permissions:
  contents: read

concurrency:
  group: check-${{ github.ref }}
  cancel-in-progress: true

jobs:
  frontend:
    name: frontend (${{ matrix.node }})
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: app
    strategy:
      fail-fast: false
      matrix:
        node: ['24']

    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v4
        with:
          version: 11.24.0

      - uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node }}

      # setup-node sin pnpm-cache kjører før storen finnes og feiler med
      # "Some specified paths were not resolved". Cacher storen selv i stedet.
      - name: resolve pnpm store
        id: store
        run: echo "path=$(pnpm store path --silent)" >> "$GITHUB_OUTPUT"

      - uses: actions/cache@v4
        with:
          path: ${{ steps.store.outputs.path }}
          key: pnpm-${{ runner.os }}-${{ hashFiles('app/pnpm-lock.yaml') }}
          restore-keys: pnpm-${{ runner.os }}-

      # --frozen-lockfile: CI skal aldri stilltiende endre avhengigheter.
      - name: install
        run: pnpm install --frozen-lockfile

      # Samme inngangspunkt som kjøres lokalt — ett sted, ikke to sannheter.
      - name: check
        run: pnpm check

      - name: upload build
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: dist-node${{ matrix.node }}
          path: app/dist
          retention-days: 7

  backend:
    name: backend
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: install ruff
        run: pipx install ruff

      - name: lint
        run: ruff check .

      - name: format
        run: ruff format --check .

      # Fanger syntaksfeil uten å starte tjenesten.
      - name: compile
        run: python -m compileall -q .

      - name: check
        run: python check.py

      # SECURITY.md: external SQLite sources are opened read-only. Axey's own
      # observation store is a writable, owned store and is not covered here.
      - name: verify read-only source access
        run: |
          if grep -nE '\b(INSERT|UPDATE|DELETE|DROP|ALTER)\b' source_readers.py; then
            echo "::error::writing SQL found — source readers must be read-only"
            exit 1
          fi
          if ! grep -q 'mode=ro' source_readers.py; then
            echo "::error::no mode=ro in source_readers.py — connections must be read-only"
            exit 1
          fi
          echo "read-only confirmed"
